data/
phase0/output/
*.sqlite
# 例外：健保藥品品項快取（開放資料非個資，隨 bundle 發佈）
!app/src-tauri/resources/drug_items.sqlite
*.db

# Spectra app data
.spectra/
*.rej
*.orig
.DS_Store
.gstack/
__pycache__/
*.pyc
.claude/worktrees/
# distill 產物：接班技能含開發過程敘述，照 2026-08-14 結構性隔離決策不入公開庫
.claude/skills/
openspec/.vector-search.db*

# Tauri app
app/node_modules/
app/src-tauri/target/
app/src-tauri/gen/
app/src-tauri/icons/android/
app/src-tauri/icons/ios/

# 開發過程紀錄不入公開庫（2026-08-14 決定）
# 這些檔案的存在目的就是記錄實測，而實測來源是家庭成員的真實健康資料。
# 個資外流不可逆，設計脈絡不公開則隨時可逆（清洗後放回即可），因此選
# 結構性隔離而非偵測型守衛：偵測擋得住數字，擋不住「所有 session 都在
# 20:00 至 22:00 開始」這種敘述型的健康資訊。
# 本地檔案完全保留，且從此可以寫真實數字，不必再為公開而犧牲精確度。
openspec/changes/
docs/verification/
docs/spikes/
docs/*handoff*.md
docs/20260808_phase0_findings.md

# 工具產物（勿入庫）
.omo/
.playwright-mcp/
