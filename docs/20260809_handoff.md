# 交接文件（2026-08-09）

## 現況一句話

MVP 已可日常使用：`mhb import`（健保存摺 JSON＋Apple Health，自動判型、
冪等累加）→ 單檔互動 dashboard（健康摘要卡設計、四分頁＋搜尋、
匯入紀錄）。64 pytest 全綠，E2E 冪等演練 PASS，已部署於使用者 iPad/Mac
實際使用中。

## 立即待辦（依序）——**已於 2026-08-09 全數完成**，詳見下方勾註與
docs/verification/{jenny_audit,karen_reality}.md；下一輪主題：Tauri App 化
（新 change，先跑 common-ground；design.md D3 已預留前端原樣遷入路徑）

1. ✅ **iPad 最終走查確認**：最新檔 `data/dashboard_20260809-private.html`
   在 iPad Safari 再走一輪（本輪 UX 迭代後尚未實機確認）。
   確認後把 tasks.md 的 7.3 打勾。
2. ✅ **spec 增補**（8 條新 requirement 已回寫並隨 archive 轉正）：UX 迭代新增的行為超出原 specs 範圍，
   需補進 `openspec/changes/mvp-core-dashboard/specs/`：
   - dashboard-generator：篩選連動、用藥三分類（品項檔命中/中醫節區/其他）、
     處方時間軸展開、搜尋結果跳轉與捲動定位、匯入紀錄卡
   - health-database：schema v2（source_documents.import_stats）、
     MIGRATIONS 前向遷移機制（原 spec 的 schema 版本化 requirement 已有
     scenario，實作已對齊，補 MODIFIED 說明即可）
   - 可用 `spectra drift mvp-core-dashboard` 輔助偵測落差
3. ✅ **收尾審計**：/Jenny（spec 符合度）→ /karen（現實檢核）→ 發現回修。
4. ✅ **結案**（specs 轉正 33 requirements、CHANGELOG 0.2.0、bin/mhb 啟動器）：`spectra archive mvp-core-dashboard`（specs 轉正、change 歸檔），
   然後更新 README 版本與 CHANGELOG（0.1.0 → 0.2.0，含 UX 迭代與匯入紀錄）。

## v2 backlog（已明確裁定延後）

- 手動補充資料（manual.json：家用量測、筆記、問醫師清單）
- 回診摘要匯出（勾選範圍的輕量分享檔，不含完整病歷）
- 中醫用藥 join 健保中藥品項檔（現顯示原始名稱）
- knowledge 條目逐項深化（現為名稱正規化＋邊界提醒層級；PS、B/C 兩項
  檢驗名身分待查證後收錄；eGFR Male 的公式歸屬待查證）
- 健保 XML 的 r8 報告換行補充（JSON 版換行被官方移除）
- 多 profile 操作介面（schema 已預留 profile_id）
- Tauri 桌面 App 包裝（前端可原樣遷入）
- 健保「非醫療類」下載檔（健保卡狀況等）解析

## 每月例行（使用者操作）

1. 健康存摺網站下載醫療類 JSON（＋XML 備存）
2. iPhone 健康 App 匯出 → 傳到 Mac
3. `python3 -m src.mhb_cli import <路徑>`（每個來源各跑一次）
4. 開新產出的 `data/dashboard_YYYYMMDD-private.html`
5. 每季：`python3 -m src.mhb_cli knowledge update`（更新藥品品項快取）
6. `python3 -m src.mhb_cli quality` 看品質報告（unmapped/過時提醒）

## 關鍵檔案地圖

- 規格 SSOT：`openspec/changes/mvp-core-dashboard/`（proposal/design/6 specs/tasks）
- G1/G3 審查證據：`docs/verification/g3_*.md`、proposal.md 末尾審查修正節
- 格式研究：`docs/20260808_phase0_findings.md`（健保 14 節區、JSON/XML 差異、
  Apple 來源怪癖全記錄）；官方 spec PDF 在 `docs/specs/`
- 設計定案：`~/.gstack/projects/notoriouslab-myhealthbank/designs/
  health-dashboard-20260809/`（三變體＋approved.json；選定「健康摘要卡」）
- 個資紀律：`data/` 全目錄 gitignore；fixture 一律去識別化；
  dashboard 檔含全部個資 NEVER 外傳；commit 前跑 code-self-review 六點

## 已知殘留事項

- 生產庫的血壓序列已停止記錄（該來源停用），總覽卡標示「最近量測日」
  屬實情非 bug。
- 健保藥品品項快取 96.8MB CSV 的下載端點：
  `https://info.nhi.gov.tw/api/iode0000s01/Dataset?rId=A21030000I-E41001-001`
  （nhi.gov.tw 主站有 WAF，此 API 直連可用）。
- design 生圖工具需 OpenAI key（未配置）；本專案設計探索直接手寫 HTML
  變體效果更好，已記入 gstack learnings。
