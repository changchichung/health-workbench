# Changelog

## 未發布（開發中）

- 趨勢圖時間軸改為時間比例：x 座標依實際日期在顯示區間內的位置定位，
  不再依「第幾個有資料的日期」等距排列。原本停止記錄的序列會畫在圖的
  最右端看似最新量測（真實資料實測：末筆距今 數百天的血壓畫在繪圖區
  右緣），只有 3 筆的檢驗項目也被鋪滿整個圖寬看似連續趨勢
- 趨勢頁新增時間區間選擇（近三月／近一年／全部），一組按鈕作用於全頁
  四張圖以維持同期對照；預設依資料新舊自動決定；單張圖在該區間內無
  資料時顯示提示與「看全部」入口
- x 軸刻度改依時間挑選（年／季／月／雙週），粒度逐級下降至不超過上限，
  避免改為時間定位後標籤重疊
- 各序列名稱與最新值改標於繪圖區右側固定位置（原本貼在折線末端，
  會被右邊界截斷，或在停止記錄的序列上壓住其他資料）
- 密集序列的標記依區間內點數兩段降級，超過門檻只畫折線不畫標記
  （真實資料實測 SVG 節點數大幅 → 332）；健保成健的獨立標記不受影響
- 步數圖粒度隨區間變化（近三月逐日、其餘月平均）
- 日期無法識別的資料點（如 null）在繪製前剔除並於圖說標示筆數；
  原本一筆 null 就會把時間軸下界拉到 1970 且不會報錯
- `generated_at` 改用本機日期：原本 App 端用 UTC 日期、Python CLI 用
  本機日期，台北時區每天有八小時兩邊差一日；時間軸以它為上界後這個
  落差會直接反映在圖上

## 0.5.0（2026-08-11）誤歸屬救援與備份

- 誤歸屬救援（匯錯成員的資料救得回來，原「刪整個成員重建」的唯一
  永久遺失路徑落幕）：匯入紀錄卡每筆來源檔案可「改歸屬」（連同全部
  資料列搬給正確成員；與目標既有紀錄重複者自動合併）或「刪除」
  （連帶該檔全部資料列，檔案可重新匯入）；操作前明細預覽＋確認，
  單一交易、中斷整批回滾
- 同人多檔重疊防護：刪除／改歸屬可能波及其他檔案也含有的紀錄時，
  預覽顯示警告（健保滾動視窗與 Apple 累積匯出天然重疊）
- 健保身分綁定守恆：健保檔只能改歸屬給未綁定身分證的成員；來源
  成員失去最後一份健保檔時自動解綁（改歸屬同時轉綁目標），誤綁
  的身分證不再卡住正主
- 匯出資料庫檔：管理成員面板進階區一鍵備份整庫（一致性快照、
  預設檔名含日期、同名拒絕），新電腦用「匯入既有資料庫檔」接回
- 仿單等外部連結修復：App 內點擊改開系統瀏覽器（原 target=_blank
  被 WebView 靜默攔下）；說明卡 GitHub 連結由「複製」改直接開啟
- 紅隊護欄擴充：救援操作中途失敗兩情境進 CI 對抗矩陣（全庫逐位元
  組不變斷言）；HTML 轉義一致性守衛（涵蓋屬性位置注入面）；
  node:test 128 項全綠
- 修正：只有單一來源資料（僅健保或僅 Apple）時，趨勢頁折線圖對
  空序列拋錯導致整個檢視空白（含匯出 HTML）；補檢視器錯誤邊界，
  單一分頁故障不再拖垮全部分頁；新增單一來源全分頁渲染回歸測試
- 匯出 HTML 的載入占位文字改為說明「這個開啟方式不執行網頁程式」並
  指到 iOS 檢視器 App（原文字讓人誤判成壞檔）
- 匯出 HTML 手機版面：窄螢幕總覽單欄、寬表格各自橫向捲動不再撐破
  整頁（診間用手機看的情境）；觸控目標維持 44px
- macOS DMG 內附「使用說明（請先閱讀）.txt」：放行、匯入、匯出與
  iOS 檢視器指引，不必先讀 GitHub 才會用；產出後掛載斷言說明檔與
  App 都在，同一支腳本在每次推 main 的 CI 就實跑
- README：補 iPhone／iPad 開啟匯出檔的方式（iOS 無法用 Safari 直接
  開本機 HTML）、四個分頁的畫面截圖、核心特色與參與開發指引
- DMG 說明檔圖示明確配位：原本交給 Finder 自動配位會落到視窗外
  （實測使用者打開 DMG 沒看到），改為指定座標並放大視窗，產出後
  斷言座標存在且落在可見範圍
- Windows 使用說明（SmartScreen 放行、%APPDATA% 資料位置）以獨立
  資產掛在 release 下載頁（安裝檔是單一執行檔，沒有可放說明的容器）
- 新增 `scripts/gen_demo_data.mjs`：產生合成示範資料與示範頁面
  （決定性輸出，README 截圖即由它產生；無真實個資，也可直接試用）
- 零 schema 變更；Python CLI 維持凍結

## 0.4.0（2026-08-10）多人資料管理

- 成員管理：新增／改名／刪除（刪除連帶該成員全部資料，輸入名稱二次
  確認；刪除後原始檔可重新匯入給其他成員）
- 匯入歸屬：每次明確選擇成員（無預設、可就地新增）；健保檔遮罩身分證
  作為驗證護欄（不符或已屬他人即阻擋零寫入），Apple 檔由面板顯著
  顯示歸屬供確認
- 依人檢視：header 成員切換器（跨啟動記憶），四分頁、搜尋、狀態列
  跟隨當前成員；儀表板標題帶成員名；匯入紀錄卡維持全庫、依成員分組
- 匯出單檔 HTML＝僅當前成員，檔名含成員名，任何裝置瀏覽器可開
- 匯入非破壞性紅隊護欄進 CI：七情境對抗矩陣（中途故障／歸戶不符／
  跨成員重複／畸形檔等），既有資料逐位元組不變、白名單斷言＋常駐
  負向自檢
- 介面打磨（走查回饋四輪）：匯入頁頂部說明卡（含 GitHub 連結）、
  通用選檔鈕、對話框記憶上次目錄、Apple 資料夾自動下潛一層、
  「匯入既有資料庫檔」降級收進管理面板進階區、暫時提示獨立通知列
- 零 schema 變更：既有資料庫直接沿用；Python CLI 維持凍結（單人
  oracle 與開發者路徑）

## 0.3.0（2026-08-10）Tauri 桌面 App（產品化）

- 桌面 App（macOS/Windows）：拖放或選檔匯入、自動判型、進度與結果
  報告卡、匯入/檢視雙分頁、資料庫與匯入紀錄卡、單檔 HTML 匯出
- 匯入引擎 JS 重寫並以差分對帳驗收：與 Python 管線對真實資料全表
  全等（數十萬 筆 Apple＋健保全節區）；新增健保 XML 匯入（與 JSON
  跨格式對帳，白名單僅 r8 換行語意，真檔 7/7 驗證）
- 差分護欄實戰戰果：抓到並修復串流掃描 chunk 邊界丟紀錄 bug、
  tauri-plugin-sql 連線池幽靈讀（棄用改自有 rusqlite 單連線橋）
- 防護沿用並前置到介面：重複檔自動跳過、歸戶確認整合進匯入確認面板、
  不同人檔案阻擋零寫入、部分失敗續行明細
- CI：GitHub Actions 測試＋守衛＋雙平台建置（產物未簽章；Windows
  實機冒煙與簽章列分發前 backlog）
- Python CLI 凍結新功能，轉為開發者路徑與差分驗收 oracle

## 0.2.0（2026-08-09）設計改版與 UX 迭代

- 視覺改版「健康摘要卡」：洞察式總覽（最新值＋變化量）、Apple 健康風卡片、深淺色
- 時間軸篩選連動；用藥三分類（藥品/中醫用藥/診療項目）＋處方時間軸展開
- 藥↔看診雙向跳轉、搜尋結果全面可點選並捲動定位
- 匯入紀錄：每次匯入的時間/檔案/新增統計（schema v3＋前向遷移機制）
- 韌性：單筆壞紀錄續行不中止整批（parse_errors 進品質報告）
- bin/mhb 啟動器（任意目錄可執行）
- 審計：Jenny 規格符合度（5 發現全修）＋Karen 現實檢核（1 發現已修），
  紀錄於 docs/verification/

## 0.1.0（2026-08-08）MVP 核心四件套

- `mhb` CLI：import（自動判型、冪等）/ rebuild / status / quality / knowledge update
- 健保存摺醫療類 JSON adapter：14 節區、藥局調劑回退、醫囑對帳、遮罩身分證歸戶
- Apple Health adapter：串流解析、來源別單位修正、epoch/離群旗標、檔內去重
- 跨批次累加合併：內容指紋（D1）、視窗接續、疑似改版偵測、步數防雙計
- knowledge：labs.yaml 40 條（附來源與引用日期）、健保藥品品項快取 join、
  非結論式用語建置檢查
- 單檔互動 dashboard：總覽/時間軸/用藥/趨勢＋全文搜尋，離線可用、
  深淺色、<10MB 閘門、繁體中文
