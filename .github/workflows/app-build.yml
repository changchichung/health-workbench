# tauri-desktop-app CI（design D6）：測試＋守衛 → 雙平台建置。
# 個資紀律：CI 僅用去識別化 fixtures，一律不得引用個資目錄。
name: app-build

on:
  push:
    branches: [main]
    paths: ["app/**", "src/**", "tests/**", "scripts/**", "packaging/**",
            ".github/workflows/app-build.yml"]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: 安裝 Python 依賴（oracle 端）
        run: pip install pyyaml
      - name: 守衛：src-tauri 業務邏輯零命中
        run: |
          ! grep -rEn "parse|adapter|schema|fingerprint|knowledge|quality_flag" app/src-tauri/src/
      - name: 守衛：workflow 不引用個資目錄
        run: |
          ! grep -rn "d""ata/" .github/workflows/
      - name: node 測試全套（含 parity fixture、禁用詞、labs.json 新鮮度）
        working-directory: app
        run: |
          npm ci
          npm test

  build:
    needs: test
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            bundle_glob: |
              app/src-tauri/target/release/bundle/dmg/*.dmg
              app/src-tauri/target/release/bundle/macos/*.app/**
          - platform: windows-latest
            bundle_glob: |
              app/src-tauri/target/release/bundle/nsis/*.exe
              app/src-tauri/target/release/bundle/msi/*.msi
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - uses: dtolnay/rust-toolchain@stable
      - uses: swatinem/rust-cache@v2
        with:
          workspaces: app/src-tauri
      - name: npm 依賴
        working-directory: app
        run: npm ci
      - name: Tauri 建置（不簽章）
        uses: tauri-apps/tauri-action@v0
        with:
          projectPath: app
      # 與 release.yml 同三支腳本（定位／塞說明檔／驗收）：每次推 main
      # 就在真實產物上實跑，發布當天才第一次執行的腳本等於沒有防線。
      # 驗收跑 unsigned 負向對照：斷言未簽章建置「不是」Developer ID
      # 簽章且無公證票據，同時 dogfood 驗收腳本本身的掛載與比對機制。
      - name: DMG 內附使用說明＋未簽章驗收（僅 macOS）
        if: matrix.platform == 'macos-latest'
        run: |
          DMG="$(bash scripts/locate_dmg.sh)"
          bash scripts/dmg_add_readme.sh "$DMG"
          bash scripts/verify_macos_signing.sh "$DMG" unsigned
      - name: 上傳安裝包
        uses: actions/upload-artifact@v4
        with:
          name: myhealthbank-${{ matrix.platform }}
          path: ${{ matrix.bundle_glob }}
          if-no-files-found: error
